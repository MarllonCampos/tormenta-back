import { Prisma, PrismaClient } from '@prisma/client';
import { DefaultArgs } from '@prisma/client/runtime/library';

class {{pascalCase name}}Service {
  private repository: Prisma.{{lowerCaseNoSpaces name}}Delegate<DefaultArgs>;
  constructor() {
    const prisma = new PrismaClient();
    this.repository = prisma.{{lowerCaseNoSpaces name}};
  }

  index = async () => {
    const {{lowerCaseNoSpaces name}} = await this.repository.findMany();
    return {{lowerCaseNoSpaces name}};
  };
   show = async () => {
    return null;
  }
  create = async ()  =>{
    return null;
  }
  update= async  () => {
    return null;
  }
  delete = async () => {
    return null;
  }
}

export default {{pascalCase name}}Service;
